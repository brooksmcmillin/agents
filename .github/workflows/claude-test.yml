name: Claude Test (Inline)

on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - "**/*.py"

jobs:
  claude-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # Try v4 instead of v6
        with:
          fetch-depth: 1

      - name: Test - Just echo
        run: |
          echo "Testing if workflow runs at all"
          echo "PR number: ${{ github.event.pull_request.number }}"

      # Commented out the actual Claude action for now
      # - name: Run Claude Code Review
      #   uses: anthropics/claude-code-action@v1
      #   with:
      #     claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
      #     prompt: "Test review"
