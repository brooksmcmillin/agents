#!/usr/bin/env bash
# Code review batch runner
# Usage: ./bin/code-review /path/to/review [--parallel] [--agents security,deps]

set -e

cd "$(dirname "$0")/.."
exec uv run python -m agents.code_reviewer.main "$@"
