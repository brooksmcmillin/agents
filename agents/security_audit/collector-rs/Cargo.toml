[package]
name = "security-audit-collector"
version = "0.1.0"
edition = "2021"
description = "Zero-dependency security audit collector for Linux systems"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
