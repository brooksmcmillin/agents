# API Base URL for production builds (optional, defaults to current origin)
# VITE_API_BASE_URL=http://localhost:8080

# API URL for dev server proxy (optional, defaults to http://localhost:8080)
# Use this if backend is running on a different machine
# VITE_API_URL=http://192.168.1.100:8080
