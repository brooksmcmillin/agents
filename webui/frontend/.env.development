# Vite development environment variables
# This file is used by Vite dev server

# API proxy target (overridden by docker-compose.dev.yml in Docker)
# Use Docker service name when running in containers
VITE_API_URL=http://backend:8080
